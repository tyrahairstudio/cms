{
  "name": "tyra-hair-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "serve": "py -m http.server 8788 --directory public",
    "seo:build": "node scripts/generate-seo-pages.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "deploy": "wrangler pages deploy public --project-name tyrahairstudio --branch main",
    "deploy:npx": "npx wrangler pages deploy public --project-name tyrahairstudio --branch main"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "wrangler": "latest"
  }
}
